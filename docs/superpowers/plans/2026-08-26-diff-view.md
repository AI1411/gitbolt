# Diff View (Issue #13) Implementation Plan

> **For agentic workers:** Inline execution.

**Goal:** Polish Diff view: Unified/Split toggle, hunk navigation (`[` / `]`), lightweight syntax coloring, binary/large-file guards, and cache hits for instant re-display.

**Architecture:** Extend `DiffView` UI + `NavigateHunk`. Optional simple token coloring without heavy highlighter crates. `AppSession`/executor consults `Cache` for `DiffContent` keyed by HEAD+path+staged.

**Proceeding with Inline Execution.**
