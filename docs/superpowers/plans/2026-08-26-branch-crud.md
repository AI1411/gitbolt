# Branch CRUD (Issue #17) Implementation Plan

> **For agentic workers:** Inline execution.

**Goal:** Branch list (local/remote), create / checkout / delete with Preflight and delete confirmation; checkout invalidates HEAD caches and reloads only status/branches/history.

**Proceeding with Inline Execution.**
