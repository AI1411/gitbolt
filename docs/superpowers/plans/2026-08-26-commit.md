# Commit (Issue #15) Implementation Plan

> **For agentic workers:** Inline execution.

**Goal:** Commit message box + validate + `git commit` with message preserved on failure; HEAD/Changes refresh on success.

**Proceeding with Inline Execution.**
