# Instant Worktree (Issue #21) Implementation Plan

> **For agentic workers:** Inline execution.

**Goal:** On Branches with a selected branch, W creates a worktree at the default path (validate → create); optional open after create.

**Proceeding with Inline Execution.**
