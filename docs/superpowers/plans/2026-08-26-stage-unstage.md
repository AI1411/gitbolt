# Stage / Unstage (Issue #14) Implementation Plan

> **For agentic workers:** Inline execution.

**Goal:** Complete Space / file / all / hunk stage-unstage with optimistic UI already partially present.

**Tasks:** Wire `StageAll`/`UnstageAll` in executor; Space toggles selected file; Stage focused hunk; Changes UI actions; keep optimistic rollback.

**Proceeding with Inline Execution.**
