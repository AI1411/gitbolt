# File History / Line History (Issue #23) Implementation Plan

> **For agentic workers:** Inline execution.

**Goal:** File-scoped history (`--follow`), line-scoped history (`-L`), lazy paging, History view + Context Panel integration, one-click access from Changes (H) and Blame (Shift+click).

**Proceeding with Inline Execution.**
