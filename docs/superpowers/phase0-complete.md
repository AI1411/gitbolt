# Phase 0 complete

Foundation issues landed via:

- #7 Filesystem watcher + cache invalidation — PR #44
- #8 CI setup (fmt / clippy / test / macOS build) — PR #45

This note exists so those issues can be closed with a merge that references them.
